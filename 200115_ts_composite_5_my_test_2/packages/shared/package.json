{
  "name": "shared",
  "version": "1.0.0",
  "main": "dist/index",
  "types": "dist/index",
  "license": "MIT",
  "scripts": {
    "build": "yarn clean && yarn compile",
    "clean": "rm -rf ./dist && rm -rf tsconfig.tsbuildinfo",
    "compile": "tsc -b tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^3.7.4"
  }
}
