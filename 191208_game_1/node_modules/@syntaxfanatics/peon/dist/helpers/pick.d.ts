import { AnElementOf } from './helper-types';
/**
 * @description
 * Clone an object and pick the desired properties
 *
 * Place put the properties to pick as the first argument
 *
 * Composes with unary functions
 *
 * @param include
 */
export declare function pick<T extends (string | number)[]>(...include: T): <R extends Record<string | number, any>>(record: R) => Pick<R, AnElementOf<T>>;
