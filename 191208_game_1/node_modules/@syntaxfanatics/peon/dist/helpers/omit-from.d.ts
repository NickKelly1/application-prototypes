/**
 * @description
 * Clone an object, omitting the undesired properties
 *
 * @param data
 */
export declare function omitFrom<R extends Record<PropertyKey, any>>(data: R): <K extends keyof R>(...toOmit: K[]) => Pick<R, Exclude<keyof R, K>>;
