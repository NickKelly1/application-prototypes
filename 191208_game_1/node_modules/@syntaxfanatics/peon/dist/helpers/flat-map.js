"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const flatten_1 = require("./flatten");
/**
 * @description
 * Map and flatten a 2 dimensional array (for environments that don't support Array.prototype.flatMap)
 *
 * @param twoDArray
 */
function flatMap(twoDArray) {
    return function doFlatMap(projectionFn) {
        const mapped = twoDArray.map(projectionFn);
        const result = flatten_1.flatten(mapped);
        return result;
    };
}
exports.flatMap = flatMap;
