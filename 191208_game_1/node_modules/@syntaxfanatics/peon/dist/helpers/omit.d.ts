import { AnElementOf } from './helper-types';
/**
 * @description
 * Clone an object and omit the desired properties
 *
 * Place put the properties to omit as the first argument
 *
 * Composes with unary functions
 *
 * @param leaveOut
 */
export declare function omit<T extends PropertyKey[]>(...leaveOut: T): <R extends Record<string | number | symbol, any>>(record: R) => Pick<R, Exclude<keyof R, AnElementOf<T>>>;
