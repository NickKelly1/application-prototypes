"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
/**
 * @description
 * Select keys to pick from an object
 * Return them in an array, in the order they were picked in
 *
 * @param extract
 */
exports.pickIntoArray = (...extract) => {
    return function doPickArray(record) {
        const result = extract.map((ex) => record[ex]);
        return result;
    };
};
