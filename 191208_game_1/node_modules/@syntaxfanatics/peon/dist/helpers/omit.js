"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const object_entries_1 = require("./object-entries");
const object_from_entries_1 = require("./object-from-entries");
/**
 * @description
 * Clone an object and omit the desired properties
 *
 * Place put the properties to omit as the first argument
 *
 * Composes with unary functions
 *
 * @param leaveOut
 */
function omit(...leaveOut) {
    return function doOmit(record) {
        const entries = object_entries_1.objectEntries(record).filter(([k]) => !leaveOut.includes(k));
        const result = object_from_entries_1.objectFromEntries(entries);
        return result;
    };
}
exports.omit = omit;
