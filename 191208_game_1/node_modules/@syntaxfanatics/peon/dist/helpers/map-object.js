"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const object_entries_1 = require("./object-entries");
const object_from_entries_1 = require("./object-from-entries");
/**
 * @description
 * Map an object
 *
 * TODO: fix typing
 *
 * @param obj
 */
function mapObject(obj) {
    return function doMapObject(projectionFn) {
        const entries = object_entries_1.objectEntries(obj);
        const mappedEntries = entries.map(([k, v]) => [k, projectionFn(v)]);
        const mappedObject = object_from_entries_1.objectFromEntries(mappedEntries);
        return mappedObject;
    };
}
exports.mapObject = mapObject;
