import { AnyFunc } from './helper-types';
/**
 * Retry an async function if it fails
 *
 * @param options
 */
export declare function retryEvery(options: {
    millisBetween: number;
    maxAttempts: number;
    log?: ('attempts' | 'errors')[];
}): <F extends AnyFunc>(tryMe: F) => <A extends Parameters<F>>(...args: A) => Promise<F extends (...args: A) => Promise<infer PR> ? PR : F extends (...args: A) => infer R ? R : never>;
