"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
const object_from_entries_1 = require("./object-from-entries");
const object_entries_1 = require("./object-entries");
exports.nullIfUndefined = (v) => {
    if (v === undefined)
        return null;
    return v;
};
/**
 * @description
 * Resolve the promise to null if it were to be undefined
 *
 * @param v
 */
exports.asyncNullIfUndefined = (v) => __awaiter(void 0, void 0, void 0, function* () {
    const innerValue = yield v;
    if (innerValue === undefined)
        return null;
    return innerValue;
});
/**
 * @description
 * Return an object where any undefined values are converted to null
 *
 * @param input
 */
function objectNullIfUndefined(input) {
    const entries = object_entries_1.objectEntries(input);
    const mappedEntries = entries.map(([k, v]) => [k, v === undefined ? null : v]);
    const result = object_from_entries_1.objectFromEntries(mappedEntries);
    return result;
}
exports.objectNullIfUndefined = objectNullIfUndefined;
