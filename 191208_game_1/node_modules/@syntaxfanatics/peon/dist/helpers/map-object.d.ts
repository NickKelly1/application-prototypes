/**
 * @description
 * Map an object
 *
 * TODO: fix typing
 *
 * @param obj
 */
export declare function mapObject<T extends Record<PropertyKey, any>>(obj: T): <F extends (entry: [keyof T, T[keyof T]]) => any>(projectionFn: F) => { [K in keyof T]: F extends (entry: [K, T[K]]) => infer R ? R : never; };
