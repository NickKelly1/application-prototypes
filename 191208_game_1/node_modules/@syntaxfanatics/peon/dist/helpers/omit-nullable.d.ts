import { OmitWhereNullable } from './helper-types';
/**
 * @description
 * Omit keys from an object whose values match any of the specified values
 *
 * Place put the properties and values to omit as the first argument
 *
 * Composes with unary functions
 *
 * @param checkKeys
 * @param value
 */
export declare function omitNullable<K extends keyof any>(...checkKeys: K[]): <R extends Record<K, any>>(fromRecord: R) => OmitWhereNullable<R, K>;
