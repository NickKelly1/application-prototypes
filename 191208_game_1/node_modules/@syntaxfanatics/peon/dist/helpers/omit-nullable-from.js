"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const object_from_entries_1 = require("./object-from-entries");
const object_entries_1 = require("./object-entries");
/**
 * @description
 * Omit keys from an object whose values match any of the specified values
 *
 * @param fromRecord
 */
function omitNullableFrom(fromRecord) {
    return function doOmitNullableFrom(...keys) {
        const entries = object_entries_1.objectEntries(fromRecord).filter(([k, v]) => !(keys.includes(k) && v !== v));
        const result = object_from_entries_1.objectFromEntries(entries);
        return result;
    };
}
exports.omitNullableFrom = omitNullableFrom;
