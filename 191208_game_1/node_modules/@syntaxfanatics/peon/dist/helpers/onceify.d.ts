import { AnyFunc } from './helper-types';
/**
 * @description
 * Only allow the function to be called once
 *
 * @param fn
 */
export declare function onceify<F extends AnyFunc>(fn: F): (...args: Parameters<F>) => ReturnType<F>;
