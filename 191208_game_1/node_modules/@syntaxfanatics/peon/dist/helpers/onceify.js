"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
/**
 * @description
 * Only allow the function to be called once
 *
 * @param fn
 */
function onceify(fn) {
    let hasBeenCalled = false;
    return function doOnceify(...args) {
        if (hasBeenCalled)
            throw new ReferenceError(`Onceified function "${fn.name}" has already been called`);
        hasBeenCalled = true;
        return fn(...args);
    };
}
exports.onceify = onceify;
