"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const object_from_entries_1 = require("./object-from-entries");
const object_entries_1 = require("./object-entries");
/**
 * @description
 * Omit keys from an object whose values match any of the specified values
 *
 * Place put the properties and values to omit as the first argument
 *
 * Composes with unary functions
 *
 * @param checkKeys
 * @param value
 */
function omitNullable(...checkKeys) {
    return function doOmitNullable(fromRecord) {
        const entries = object_entries_1.objectEntries(fromRecord).filter(([k, v]) => !(checkKeys.includes(k) && (v === null || v === undefined)));
        const result = object_from_entries_1.objectFromEntries(entries);
        return result;
    };
}
exports.omitNullable = omitNullable;
