/**
 * @description
 * Create an object from the given entries (array of arrays of key-value pairs)
 *
 * @param entries
 */
export declare function objectFromEntries<K extends PropertyKey, T>(entries: [K, T][]): Record<K, T>;
